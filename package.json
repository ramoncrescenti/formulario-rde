{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "v14.17.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "build": "cd client-form && npm install && npm run build"
  },
  "author": "Ramon",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
